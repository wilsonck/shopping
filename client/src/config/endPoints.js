const localhost = "http://localhost:5000"

export default Object.freeze({
    products: `${localhost}/products/`,
    brands: `${localhost}/brands/`,
    bag: `${localhost}/bag/`,
    wishList: `${localhost}/wishList/`
});
