import React from 'react';
import classes from './Footer.module.scss';

function Footer() {
    return (
        <footer className="container">
            <p className={classes.Footer__sidenote}>Footer</p>
        </footer>
    );
}

export default Footer;